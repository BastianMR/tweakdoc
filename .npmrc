# npm falla con conflicto de peer deps de tiptap (@tiptap/extensions 3.30.2 vs 3.30.5 exigida por extension-character-count).
# legacy-peer-deps = comportamiento npm v6: no autointala/valida peers estrictos.
# Esto alinea CI y local: mismo `npm ci` sin flags extra.
legacy-peer-deps=true
